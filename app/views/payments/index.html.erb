<table>
	<tr>
		<th>Opis</th>
		<th></th>
	</tr>

<% @payments.each do |payment| %>
	<tr>
		<td><%= payment.opis %></td>
		<td><%= link_to 'Edit', edit_payment_path(payment) %> <%= link_to 'Destroy', payment, :confirm => 'Are you sure?', :method => :delete %></td>
	</tr>
<% end %>
</table>

<br />

<%= link_to 'New Payment', new_payment_path %>
